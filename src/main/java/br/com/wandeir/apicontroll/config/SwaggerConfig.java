package br.com.wandeir.apicontroll.config;


public class SwaggerConfig {
	/*
	 * @Bean public Docket api() { return new Docket(DocumentationType.SWAGGER_2)
	 * .select() .apis(RequestHandlerSelectors.basePackage(
	 * "br.com.wandeir.apicontroll.controller")) .paths(PathSelectors.any())
	 * .build(); }
	 * 
	 * private SecurityContext securityContext() { return SecurityContext.builder()
	 * .securityReferences(defaultAuth()) .forPaths(PathSelectors.ant("/pessoa/**"))
	 * .build(); }
	 * 
	 * private List<SecurityReference> defaultAuth() { AuthorizationScope
	 * authorizationScope = new AuthorizationScope("ADMIN",
	 * "be52ff56-71c4-4b04-b5e0-1613809e9e42"); AuthorizationScope[]
	 * authorizationScopes = new AuthorizationScope[1]; authorizationScopes[0] =
	 * authorizationScope; return Arrays.asList( new
	 * SecurityReference("Authorization", authorizationScopes)); }
	 */
}
